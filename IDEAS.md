# Идеи и бэклог

Что хочется сделать дальше. Не спецификация: заметки, чтобы не забыть. Сделанное переносить в раздел «Сделано» или удалять.

## Хочется

### Аналитика: следующий шаг
- История по дням и уникальные посетители: Workers Analytics Engine (`writeDataPoint` в `ctx.waitUntil`, запрос через SQL API, показывать в админке). Core Web Vitals: Cloudflare Web Analytics (бикон + CSP в `public/_headers`).
- Считать скачивания ZIP (клиент дергает лёгкий `POST /api/album/<id>/event`? или отдельное действие в `AlbumInfoDO`).

## Сделано

### При загрузке фоток сразу выбирать, в какие ещё альбомы добавить (2026-09-06)
- Пикер «Also add the uploaded photos to other albums» под формой загрузки на странице альбома. После загрузки `{ name, ref }` каждого файла прикрепляется к отмеченным альбомам через `POST album/<id>/files` чанками по 20, конфликты имён спрашиваются один раз на альбом. На форме «New album» не делал: сначала создаём альбом, потом на его странице загружаем.

### Статистика просмотров (2026-09-06)
- Сделан вариант 3: счётчики в `AlbumInfoDO` (`hit`/`stats`/`importStats`/`resetStats`), инкремент из `src/api/album.js` в `ctx.waitUntil`. Считаются только успешные открытия, по каждой ссылке и суммарно, с датой начала отсчёта и последнего визита. В админке: колонка Views в списке, бейджи у ссылок и строка «since … last …» на странице альбома, кнопка Reset views. `ALBUM_VIEWS=0` выключает.
- Не сделано (возможные продолжения): история по дням и уникальные посетители (вариант 1, Workers Analytics Engine), Web Vitals через Cloudflare Web Analytics (вариант 2, нужен CSP). Счётчик скачиваний ZIP.
